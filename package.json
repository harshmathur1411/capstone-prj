{
  "name": "tourvelsm",
  "version": "1.0.0",
  "scripts": {
    "start": "node tourvelsm-backend/server.js",
    "heroku-postbuild": "cd tourvelsm-frontend && npm install && npm run build && cp -r tourvelsm-frontend/build ../build"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
